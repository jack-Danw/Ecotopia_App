import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ecotopia_United',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
